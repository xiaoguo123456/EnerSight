{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "jsx": "react-jsx",
    "noEmit": true,
    // 同 core：显式声明需要的 @types，避免隐式引入全部
    "types": ["node", "@tarojs/taro"],
    "baseUrl": ".",
    "paths": { "@/*": ["./src/*"] }
  },
  "include": ["src", "config"]
}
